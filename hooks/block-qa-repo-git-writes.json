{
  "description": "Blocks the plugin's own agents from running git commit/push — QA artifacts are always reviewed and committed manually by the QA engineer.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/block-qa-repo-git-writes.sh\""
          }
        ]
      }
    ]
  }
}
